  <div>
    <h1 class="story-title">{{story.title}}</h1>
  </div>

<div class="col-md-8 col-md-offset-2">
  {{#each story.sections as |section|}}
    <p class="section-body">{{section.body}}</p>
    <p class="section-author">authored by: {{section.author}}</p>
  {{/each}}
</div>